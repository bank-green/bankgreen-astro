import { PageContent } from "@components/PageContent";

export function NotListedPage() {
  return (
    <PageContent>
      <article>
        <header>
          <h1>Bank Not Listed</h1>
        </header>

        <section>
          <p>Can't find your bank? Let us know and we'll try to add it.</p>
          {/* Form to submit unlisted bank */}
        </section>
      </article>
    </PageContent>
  );
}
