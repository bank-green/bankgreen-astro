---
import { SlicePage } from '@components/pages'
import BaseLayout from '@layouts/BaseLayout.astro'
import { getSingleSafe } from '@lib/prismic'

const page = await getSingleSafe('greenpolicyevaluatorpage')
---

<BaseLayout title="Green Policy Evaluator">
  <SlicePage page={page} client:load />
</BaseLayout>
