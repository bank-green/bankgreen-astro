---
import BaseLayout from "@layouts/BaseLayout.astro";
import { getSingleSafe } from "@lib/prismic";
import { VolunteersPage } from "@components/pages";

const page = await getSingleSafe("volunteerspage");
---

<BaseLayout title="Volunteering at Bank.Green">
  <VolunteersPage page={page} client:load />
</BaseLayout>
