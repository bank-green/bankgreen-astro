---
import { MethodologyPage } from '@components/pages'
import BaseLayout from '@layouts/BaseLayout.astro'
import { getSingleSafe } from '@lib/prismic'

export const prerender = true

const page = await getSingleSafe('methodology')
---

<BaseLayout title="Ratings Methodology">
  <MethodologyPage page={page} client:load />
</BaseLayout>
