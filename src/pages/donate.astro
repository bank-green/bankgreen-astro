---
import { DonatePage } from '@components/pages'
import BaseLayout from '@layouts/BaseLayout.astro'
import { getSingleSafe } from '@lib/prismic'

export const prerender = true

const page = await getSingleSafe('donationpage')
---

<BaseLayout title="Donate">
  <DonatePage page={page} client:load />
</BaseLayout>
